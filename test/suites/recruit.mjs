// ---- the recruiter, live (docs/architecture/10 execution) -------------------------------
// Drives recruit end-to-end: a leader musters a force by making offers; candidates weigh the offer
// by their OWN motivation and form their own join goals; the party grows. RECRUIT forced on
// in-test. Asserts the follower side is independent (a wary stranger may decline). Filled by the
// recruiter worktree.
export function recruitTest(ok, helpers) {
  // TODO(worktree recruiter): muster derivation + offer execution + follower-side join + party grows.
}
