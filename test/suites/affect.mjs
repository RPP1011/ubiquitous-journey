// ---- the Affect rows rob/free/wreck, live (docs/architecture/10 execution) ---------------
// Drives rob (conserved theft by force), free (cut a captive's bonds), and wreck (sabotage) end-
// to-end through the frame loop, with ROB/AFFECT forced on in-test. Asserts gold CONSERVED on a
// rob, the freed/wrecked flags flip, and nothing throws. Filled by the affect worktree.
export function affectTest(ok, helpers) {
  // TODO(worktree affect): rob/free/wreck execution + conservation/flag assertions.
}
