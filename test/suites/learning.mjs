// ---- the recipe-learning lifecycle, live (docs/architecture/10 execution) ----------------
// Drives observe/ask/study end-to-end: a crafter that lacks a recipe forms a learn goal, learns
// it (study at a teacher / observe a workshop), and can then produce — conserved (recipes are a
// Set; tuition moves through the market). KNOW forced on in-test. Filled by the learning worktree.
export function learningTest(ok, helpers) {
  // TODO(worktree learning): learn-goal derivation + study/observe execution + recipe acquired.
}
