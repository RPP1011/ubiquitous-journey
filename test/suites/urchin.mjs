// ---- the urchin heist, live (docs/architecture/10 execution) ----------------------------
// Drives the observe→approach→burgle heist end-to-end through the REAL frame loop, with URCHIN
// forced on in-test (restored after), and asserts: the urchin cases the mark, learns the stash,
// burgles it, gold is CONSERVED (the take debits the mark), and nothing throws. Filled by the
// urchin worktree.
export function urchinTest(ok, helpers) {
  // TODO(worktree urchin): scripted heist scenario + conservation/no-freeze assertions.
}
