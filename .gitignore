# OS / editor cruft
.DS_Store
*.swp
*~

# TypeScript emit (reproducible: `bunx tsc --noEmit && bunx tsc`)
dist/
node_modules/

# git worktrees used by parallel agents (never commit)
.claude/worktrees/

# DF observer replay logs (regenerable via test/record.mjs)
*.replay.json
replay.json
